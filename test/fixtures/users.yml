one:
  email: one@example

two:
  email: two@example

with_password:
  email: with_password@example
  encrypted_password: <%= Devise::Encryptor.digest(User, "12345678") %>

with_roles:
  email: with_roles@example
  roles: one, two
