one:
  email: one@example
  encrypted_password: <%= Devise::Encryptor.digest(User, "12345678") %>

two:
  email: two@example
  encrypted_password: <%= Devise::Encryptor.digest(User, "12345678") %>

admin:
  email: admin@example.com
  roles: admin
