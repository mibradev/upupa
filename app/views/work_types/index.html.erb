<%= render "shared/scaffold/header" %>

<% if @resources.any? %>
  <%= render "shared/scaffold/table", th_contents: ["Name", "Multiplicand"] do |resource| %>
    <%= render "shared/scaffold/table/row", resource: resource, td_contents: [resource.name, resource.multiplicand] %>
  <% end %>
<% else %>
  <%= render "shared/scaffold/empty_collection" %>
<% end %>
