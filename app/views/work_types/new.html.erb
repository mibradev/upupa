<% content_for :title, I18n.t("views.titles.new", model: WorkType.model_name.human) %>

<main class="space-y-8">
  <header>
    <h1 class="text-2xl font-bold">
      <%= yield :title %>
    </h1>
  </header>

  <%= render "form", work_type: @work_type %>

  <div class="overflow-x-auto flex gap-2">
    <%= link_to I18n.t("views.actions.back"), work_types_path, class: "block py-2 px-4 text-sm font-medium tracking-widest text-orange-100 uppercase bg-gray-700 hover:bg-gray-600 focus:bg-gray-500 focus:outline-none" %>
  </div>
</main>
