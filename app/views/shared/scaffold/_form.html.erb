<%= form_with(model: resource) do |form| %>
  <%= render "shared/scaffold/form/errors", resource: resource %>

  <%= yield form %>

  <%= render "shared/scaffold/form/submit", form: form %>
<% end %>
