<%= form_with(model: [word_count_file.word_count, word_count_file]) do |form| %>
  <div class="space-y-4">
    <% if word_count_file.errors.any? %>
      <div class="p-4 bg-red-900">
        <h4 class="mb-2 font-medium text-red-100"><%= I18n.t("views.form.errors.heading") %></h4>

        <ul class="pl-4 list-disc list-inside text-sm font-medium text-red-50">
          <% word_count_file.errors.each do |error| %>
            <li><%= error.full_message %></li>
          <% end %>
        </ul>
      </div>
    <% end %>

    <div class="space-y-2">
      <%= form.label :word_count_id, class: "block font-medium" %>
      <%= form.text_field :word_count_id, disabled: true, value: word_count_file.word_count.date, class: "block w-full focus:border-indigo-900 focus:ring-indigo-900" %>
    </div>

    <div class="space-y-2">
      <%= form.label :work_file_id, class: "block font-medium" %>
      <%= form.collection_select :work_file_id, WorkFile.all, :id, :name, {include_blank: true}, class: "block w-full focus:border-indigo-900 focus:ring-indigo-900" %>
    </div>

    <div class="space-y-2">
      <%= form.label :actual_word_count, class: "block font-medium" %>
      <%= form.number_field :actual_word_count, class: "block w-full focus:border-indigo-900 focus:ring-indigo-900" %>
    </div>

    <div class="space-y-2">
      <%= form.label :work_type_id, class: "block font-medium" %>
      <%= form.collection_select :work_type_id, WorkType.all, :id, :name, {include_blank: true}, class: "block w-full focus:border-indigo-900 focus:ring-indigo-900" %>
    </div>

    <div class="space-y-2">
      <%= form.label :notes, class: "block font-medium" %>
      <%= form.text_area :notes, class: "block w-full focus:border-indigo-900 focus:ring-indigo-900" %>
    </div>

    <%= form.button class: "py-2 px-4 text-sm font-medium tracking-wider text-indigo-100 uppercase bg-indigo-900 hover:bg-indigo-800 focus:bg-indigo-700 focus:outline-none" %>
  </div>
<% end %>
