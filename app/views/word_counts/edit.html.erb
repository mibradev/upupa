<%= page.header do |header| %>
  <%= header.heading "Editing Word Count" %>

  <%= header.actions do |action| %>
    <%= action.show @word_count %>
    <%= action.index word_counts_path %>
  <% end %>
<% end %>

<%= render "form", word_count: @word_count %>
