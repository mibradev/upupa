<% content_for :title, I18n.t("views.titles.edit", model: WordCount.model_name.human) %>

<main class="space-y-8">
  <header>
    <h1 class="text-2xl font-bold">
      <%= yield :title %>
    </h1>
  </header>

  <%= render "form", word_count: @word_count %>
</main>
